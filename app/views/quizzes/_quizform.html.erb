<%= simple_form_for @quiz do |f| %>
  <h2><%= f.label "Chilean expression" %></h2>
  <%=   f.collection_radio_buttons :options, [[true, 'Yes'], [false, 'No']], :first, :last %>
  <%= f.submit "Siguiente" %>
<% end %>
